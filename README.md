# Block Architecture

Система агентов для AI-driven разработки.

## Идея

Не методология которую читает человек. **Операционная система** которая превращает идею в работающий продукт через иерархию AI-агентов.

```
Человек (идея) → Агент-архитектор → План → Оркестратор → Агенты уровней → Результат
```

## Ключевые принципы

1. **Изоляция** — агент знает только свой блок
2. **Контракты** — чёткие форматы входа/выхода между блоками  
3. **Результаты важнее кода** — диагностика по цепочке результатов
4. **Человек — архитектор** — валидирует план, не код

## Отличие от существующего

| Существующие системы | Block Architecture |
|---------------------|-------------------|
| Симуляция ролей (PM, Dev) | Поток результатов |
| Агенты общаются | Агенты передают данные |
| Человек ревьюит код | Человек валидирует план |
| Для разработчиков | Для НЕ-разработчиков |

## Структура

```
block-architecture/
├── system/                    # Как работает система
│   ├── architect-agent.md     # Агент-архитектор
│   ├── orchestrator.md        # Оркестратор (LangGraph)
│   ├── level-agents.md        # Агенты уровней
│   └── validation-flow.md     # Валидация с человеком
│
├── contracts/                 # Спецификации форматов
│   ├── manifest-spec.md       # Формат манифеста
│   ├── block-spec.md          # Формат контракта блока
│   └── project-manifest.md    # (устаревший, см. manifest-spec)
│
├── templates/                 # Шаблоны
│   └── constitution-template.md
│
├── examples/                  # Примеры
│   └── simple-api/            # Минимальный пример
│       ├── manifest.yaml
│       ├── constitution.md
│       └── contracts/
│
├── research/                  # База знаний
│   └── knowledge-base.md
│
└── logs/                      # История развития
    ├── 2026-01-15-genesis.md
    └── 2026-01-15-system-shift.md
```

## Компоненты системы

### Агент-архитектор
Превращает идею в план:
- Понимает идею человека
- Исследует рынок и технологии
- Декомпозирует на блоки
- Создаёт манифест и контракты
- Валидирует с человеком

### Оркестратор (LangGraph)
Исполняет план:
- Читает манифест
- Запускает блоки по уровням
- Управляет параллелизмом
- Проверяет phase gates
- Собирает результаты

### Агенты уровней
Выполняют блоки:
- Знают только свой контракт
- Используют Claude Code / Cursor
- Выдают результат
- Не знают про другие блоки

## Артефакты

### manifest.yaml
Центральный файл проекта — блоки, связи, состояние.

### constitution.md  
Неизменные правила — стек, стандарты, ограничения.

### contracts/*.yaml
Контракт каждого блока — вход, выход, критерии успеха.

## Цель

**10 человек → миллиардный оборот**

Обычный человек + эта система = уровень продвинутого tech lead.

## Статус

🚧 В разработке

- [x] Концепция
- [x] Спецификации форматов
- [x] Пример проекта
- [ ] Прототип оркестратора
- [ ] Прототип агента-архитектора
- [ ] Тестирование на реальном проекте

---

*Версия 0.3 | Январь 2026*
